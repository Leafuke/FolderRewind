<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:tb="using:H.NotifyIcon">

    <XamlUICommand x:Key="ShowHideWindowCommand"
                   x:Uid="Tray_ShowHideCommand" />

    <XamlUICommand x:Key="QuitCommand"
                   x:Uid="Tray_QuitCommand" />

    <tb:TaskbarIcon
        x:Key="TrayIcon"
        x:Uid="Tray_Icon"
        ContextMenuMode="PopupMenu"
        IconSource="ms-appx:///Assets/logo.ico"
        LeftClickCommand="{StaticResource ShowHideWindowCommand}"
        NoLeftClickDelay="True"
        Visibility="Visible">
        <tb:TaskbarIcon.ContextFlyout>
            <MenuFlyout>
                <MenuFlyoutItem x:Uid="Tray_ShowHideItem"
                                Command="{StaticResource ShowHideWindowCommand}" />
                <MenuFlyoutSeparator />
                <MenuFlyoutItem x:Uid="Tray_ExitItem"
                                Command="{StaticResource QuitCommand}" />
            </MenuFlyout>
        </tb:TaskbarIcon.ContextFlyout>
    </tb:TaskbarIcon>
</ResourceDictionary>
