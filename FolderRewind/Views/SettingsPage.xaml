<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="FolderRewind.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:FolderRewind.Models"
    mc:Ignorable="d">

    <ScrollViewer Padding="24">
        <StackPanel Spacing="24" HorizontalAlignment="Stretch">

            <TextBlock x:Uid="SettingsPage_Title" Style="{ThemeResource TitleTextBlockStyle}" Margin="0,0,0,12"/>

            <!-- 1. 核心行为 -->
            <StackPanel Spacing="8">
                <TextBlock x:Uid="SettingsPage_CoreBehavior" Style="{ThemeResource SubtitleTextBlockStyle}"/>

                <Grid CornerRadius="8">
                    <Grid Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Translation="0,0,8">
                        <Grid.Shadow>
                            <ThemeShadow />
                        </Grid.Shadow>
                        <Border BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="16">
                            <StackPanel Spacing="20">
                                <!-- 开机自启 -->
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock x:Uid="SettingsPage_RunOnStartup"/>
                                        <TextBlock x:Uid="SettingsPage_RunOnStartupDesc" FontSize="12" Opacity="0.6"/>
                                    </StackPanel>
                                    <!-- 添加 Toggled 事件 -->
                                    <ToggleSwitch Grid.Column="1" IsOn="{x:Bind Settings.RunOnStartup, Mode=TwoWay}" Toggled="OnRunOnStartupToggled"/>
                                </Grid>

                                <MenuFlyoutSeparator/>

                                <!-- 检查更新 -->
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock x:Uid="SettingsPage_CheckUpdates"/>
                                        <TextBlock x:Uid="SettingsPage_CheckUpdatesDesc" FontSize="12" Opacity="0.6"/>
                                    </StackPanel>
                                    <!-- 添加 Toggled 事件 -->
                                    <ToggleSwitch Grid.Column="1" IsOn="{x:Bind Settings.CheckForUpdates, Mode=TwoWay}" Toggled="OnSettingChanged"/>
                                </Grid>
                            </StackPanel>
                        </Border>
                    </Grid>
                </Grid>
            </StackPanel>

            <!-- 2. 运行环境 (7-Zip) -->
            <StackPanel Spacing="8">
                <TextBlock x:Uid="SettingsPage_Environment" Style="{ThemeResource SubtitleTextBlockStyle}"/>
                <Grid CornerRadius="8">
                    <Grid Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Translation="0,0,8">
                        <Grid.Shadow>
                            <ThemeShadow />
                        </Grid.Shadow>
                        <Border BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="16">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Spacing="2" VerticalAlignment="Center">
                                    <TextBlock x:Uid="SettingsPage_7zPath"/>
                                    <TextBlock x:Uid="SettingsPage_7zPathDesc" FontSize="12" Opacity="0.6"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                    <TextBox Text="{x:Bind Settings.SevenZipPath, Mode=TwoWay}" Width="260" IsReadOnly="True" VerticalAlignment="Center"/>
                                    <Button x:Uid="SettingsPage_BrowseButton" Click="OnBrowse7zClick" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </Grid>
                </Grid>
            </StackPanel>

            <!-- 3. 日志与诊断 -->
            <StackPanel Spacing="8">
                <TextBlock x:Uid="SettingsPage_LogDiag" Style="{ThemeResource SubtitleTextBlockStyle}"/>
                <Grid CornerRadius="8">
                    <Grid Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Translation="0,0,8">
                        <Grid.Shadow>
                            <ThemeShadow />
                        </Grid.Shadow>
                        <Border BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="16">
                            <StackPanel Spacing="16">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Spacing="2">
                                        <TextBlock x:Uid="SettingsPage_LogEnabled" />
                                        <TextBlock x:Uid="SettingsPage_LogEnabledDesc" FontSize="12" Opacity="0.6" />
                                    </StackPanel>
                                    <ToggleSwitch Grid.Column="1" IsOn="{x:Bind Settings.EnableFileLogging, Mode=TwoWay}" Toggled="OnLoggingChanged"/>
                                </Grid>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Spacing="2">
                                        <TextBlock x:Uid="SettingsPage_MaxLogSize" />
                                        <TextBlock x:Uid="SettingsPage_MaxLogSizeDesc" FontSize="12" Opacity="0.6" />
                                    </StackPanel>
                                    <NumberBox x:Name="MaxLogSizeBox" Grid.Column="1" Width="160" Minimum="1" Maximum="50" Value="{x:Bind Settings.MaxLogFileSizeMb, Mode=TwoWay}" ValueChanged="OnLogSizeChanged"/>
                                </Grid>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Spacing="2">
                                        <TextBlock x:Uid="SettingsPage_LogRetention" />
                                        <TextBlock x:Uid="SettingsPage_LogRetentionDesc" FontSize="12" Opacity="0.6" />
                                    </StackPanel>
                                    <NumberBox x:Name="LogRetentionBox" Grid.Column="1" Width="160" Minimum="1" Maximum="60" Value="{x:Bind Settings.LogRetentionDays, Mode=TwoWay}" ValueChanged="OnRetentionChanged"/>
                                </Grid>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Spacing="2">
                                        <TextBlock x:Uid="SettingsPage_DebugLog" />
                                        <TextBlock x:Uid="SettingsPage_DebugLogDesc" FontSize="12" Opacity="0.6" />
                                    </StackPanel>
                                    <ToggleSwitch Grid.Column="1" IsOn="{x:Bind Settings.EnableDebugLogs, Mode=TwoWay}" Toggled="OnLoggingChanged"/>
                                </Grid>

                                <Button x:Uid="SettingsPage_OpenLogCenter" HorizontalAlignment="Left" Click="OnOpenLogCenterClick" />
                            </StackPanel>
                        </Border>
                    </Grid>
                </Grid>
            </StackPanel>

            <!-- 4. 插件 -->
            <StackPanel Spacing="8">
                <TextBlock x:Uid="SettingsPage_Plugins" Style="{ThemeResource SubtitleTextBlockStyle}"/>
                <Grid CornerRadius="8">
                    <Grid Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Translation="0,0,8">
                        <Grid.Shadow>
                            <ThemeShadow />
                        </Grid.Shadow>
                        <Border BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="16">
                            <StackPanel Spacing="16">

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Spacing="2">
                                        <TextBlock x:Uid="SettingsPage_PluginsEnabled"/>
                                        <TextBlock x:Uid="SettingsPage_PluginsEnabledDesc" FontSize="12" Opacity="0.6"/>
                                    </StackPanel>
                                    <ToggleSwitch x:Name="PluginsMasterSwitch" Grid.Column="1" IsOn="{x:Bind Settings.Plugins.Enabled, Mode=TwoWay}" Toggled="OnPluginsEnabledToggled"/>
                                </Grid>

                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <Button x:Uid="SettingsPage_OpenPluginStore" Click="OnOpenPluginStoreClick" IsEnabled="{x:Bind Settings.Plugins.Enabled, Mode=OneWay}"/>
                                    <Button x:Uid="SettingsPage_ManualInstallPlugin" Click="OnManualInstallPluginClick" IsEnabled="{x:Bind Settings.Plugins.Enabled, Mode=OneWay}"/>
                                    <Button x:Uid="SettingsPage_OpenPluginFolder" Click="OnOpenPluginFolderClick"/>
                                    <Button x:Uid="SettingsPage_RefreshPlugins" Click="OnRefreshPluginsClick"/>
                                </StackPanel>

                                <TextBlock x:Uid="SettingsPage_InstalledPlugins" FontWeight="SemiBold"/>

                                <ListView ItemsSource="{x:Bind InstalledPlugins, Mode=OneWay}"
                                          IsEnabled="{x:Bind Settings.Plugins.Enabled, Mode=OneWay}">
                                    <ListView.ItemTemplate>
                                        <DataTemplate x:DataType="models:InstalledPluginInfo">
                                            <Grid Padding="8">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <StackPanel Spacing="2">
                                                    <TextBlock Text="{x:Bind Name}" FontWeight="SemiBold"/>
                                                    <TextBlock Text="{x:Bind Description}" Opacity="0.7" TextWrapping="Wrap"/>
                                                    <StackPanel Orientation="Horizontal" Spacing="12">
                                                        <TextBlock Text="{x:Bind Version}" Opacity="0.7"/>
                                                        <TextBlock Text="{x:Bind Author}" Opacity="0.7"/>
                                                        <TextBlock Text="{x:Bind Id}" Opacity="0.7"/>
                                                    </StackPanel>
                                                    <TextBlock Text="{x:Bind LoadError}" Foreground="{ThemeResource SystemFillColorCriticalBrush}" Opacity="0.9" TextWrapping="Wrap"
                                                               Visibility="{x:Bind LoadError, Mode=OneWay, Converter={StaticResource StringNullOrEmptyToVisConverter}}"/>
                                                </StackPanel>

                                                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                                    <ToggleSwitch IsOn="{x:Bind IsEnabled, Mode=TwoWay}"  Margin="20,0,0,0" Toggled="OnPluginEnabledToggled"/>
                                                    <Button x:Uid="SettingsPage_PluginSettings" Click="OnPluginSettingsClick" Tag="{x:Bind}"/>
                                                    <Button x:Uid="SettingsPage_PluginUninstall" Click="OnPluginUninstallClick" Tag="{x:Bind}"/>
                                                </StackPanel>
                                            </Grid>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                </ListView>

                            </StackPanel>
                        </Border>
                    </Grid>
                </Grid>
            </StackPanel>

            <!-- 5. KnotLink 互联 -->
            <StackPanel Spacing="8">
                <TextBlock x:Uid="SettingsPage_KnotLink" Style="{ThemeResource SubtitleTextBlockStyle}"/>
                <Grid CornerRadius="8">
                    <Grid Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Translation="0,0,8">
                        <Grid.Shadow>
                            <ThemeShadow />
                        </Grid.Shadow>
                        <Border BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="16">
                            <StackPanel Spacing="16">
                                <!-- 启用 KnotLink -->
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Spacing="2">
                                        <TextBlock x:Uid="SettingsPage_KnotLinkEnabled"/>
                                        <TextBlock x:Uid="SettingsPage_KnotLinkEnabledDesc" FontSize="12" Opacity="0.6"/>
                                    </StackPanel>
                                    <ToggleSwitch x:Name="KnotLinkSwitch" Grid.Column="1" IsOn="{x:Bind Settings.EnableKnotLink, Mode=TwoWay}" Toggled="OnKnotLinkToggled"/>
                                </Grid>

                                <MenuFlyoutSeparator/>

                                <!-- 状态显示 -->
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <FontIcon Glyph="&#xE774;" FontSize="16" Foreground="{x:Bind KnotLinkStatusColor, Mode=OneWay}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <StackPanel Grid.Column="1" Spacing="2">
                                        <TextBlock x:Uid="SettingsPage_KnotLinkStatus"/>
                                        <TextBlock x:Name="KnotLinkStatusText" Text="{x:Bind KnotLinkStatusMessage, Mode=OneWay}" FontSize="12" Opacity="0.7"/>
                                    </StackPanel>
                                </Grid>

                                <!-- 高级设置 -->
                                <Expander x:Uid="SettingsPage_KnotLinkAdvanced" IsExpanded="False" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch">
                                    <StackPanel Spacing="12">
                                        <!-- 服务器地址 -->
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Spacing="2">
                                                <TextBlock x:Uid="SettingsPage_KnotLinkHost"/>
                                                <TextBlock x:Uid="SettingsPage_KnotLinkHostDesc" FontSize="12" Opacity="0.6"/>
                                            </StackPanel>
                                            <TextBox Grid.Column="1" Text="{x:Bind Settings.KnotLinkHost, Mode=TwoWay}" Width="160" TextChanged="OnKnotLinkSettingChanged"/>
                                        </Grid>

                                        <!-- AppId -->
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Spacing="2">
                                                <TextBlock x:Uid="SettingsPage_KnotLinkAppId"/>
                                                <TextBlock x:Uid="SettingsPage_KnotLinkAppIdDesc" FontSize="12" Opacity="0.6"/>
                                            </StackPanel>
                                            <TextBox Grid.Column="1" Text="{x:Bind Settings.KnotLinkAppId, Mode=TwoWay}" Width="160" TextChanged="OnKnotLinkSettingChanged"/>
                                        </Grid>

                                        <!-- OpenSocket ID -->
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Spacing="2">
                                                <TextBlock x:Uid="SettingsPage_KnotLinkOpenSocketId"/>
                                                <TextBlock x:Uid="SettingsPage_KnotLinkOpenSocketIdDesc" FontSize="12" Opacity="0.6"/>
                                            </StackPanel>
                                            <TextBox Grid.Column="1" Text="{x:Bind Settings.KnotLinkOpenSocketId, Mode=TwoWay}" Width="160" TextChanged="OnKnotLinkSettingChanged"/>
                                        </Grid>

                                        <!-- Signal ID -->
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Spacing="2">
                                                <TextBlock x:Uid="SettingsPage_KnotLinkSignalId"/>
                                                <TextBlock x:Uid="SettingsPage_KnotLinkSignalIdDesc" FontSize="12" Opacity="0.6"/>
                                            </StackPanel>
                                            <TextBox Grid.Column="1" Text="{x:Bind Settings.KnotLinkSignalId, Mode=TwoWay}" Width="160" TextChanged="OnKnotLinkSettingChanged"/>
                                        </Grid>

                                        <!-- 重置默认按钮 -->
                                        <Button x:Uid="SettingsPage_KnotLinkResetDefault" HorizontalAlignment="Left" Click="OnKnotLinkResetClick"/>
                                    </StackPanel>
                                </Expander>

                                <!-- 操作按钮 -->
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <Button x:Uid="SettingsPage_KnotLinkRestart" Click="OnKnotLinkRestartClick" IsEnabled="{x:Bind Settings.EnableKnotLink, Mode=OneWay}"/>
                                    <Button x:Uid="SettingsPage_KnotLinkTest" Click="OnKnotLinkTestClick" IsEnabled="{x:Bind Settings.EnableKnotLink, Mode=OneWay}"/>
                                </StackPanel>

                                <!-- 命令参考 -->
                                <Expander x:Uid="SettingsPage_KnotLinkCommands" IsExpanded="False" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch">
                                    <StackPanel Spacing="8">
                                        <TextBlock x:Uid="SettingsPage_KnotLinkCommandsDesc" TextWrapping="Wrap" Opacity="0.8"/>
                                        <RichTextBlock FontFamily="Consolas" FontSize="12" IsTextSelectionEnabled="True">
                                            <Paragraph>
                                                <Run Text="LIST_CONFIGS" FontWeight="SemiBold"/><LineBreak/>
                                                <Run Text="LIST_FOLDERS &lt;config_id&gt;"/><LineBreak/>
                                                <Run Text="LIST_BACKUPS &lt;config_id&gt; &lt;folder&gt;"/><LineBreak/>
                                                <Run Text="GET_CONFIG &lt;config_id&gt;"/><LineBreak/>
                                                <Run Text="SET_CONFIG &lt;config_id&gt; &lt;key&gt; &lt;value&gt;"/><LineBreak/>
                                                <Run Text="BACKUP &lt;config_id&gt; &lt;folder&gt; [comment]"/><LineBreak/>
                                                <Run Text="RESTORE &lt;config_id&gt; &lt;folder&gt; &lt;file&gt;"/><LineBreak/>
                                                <Run Text="BACKUP_ALL &lt;config_id&gt; [comment]"/><LineBreak/>
                                                <Run Text="AUTO_BACKUP &lt;config_id&gt; &lt;folder&gt; &lt;minutes&gt;"/><LineBreak/>
                                                <Run Text="STOP_AUTO_BACKUP &lt;config_id&gt; &lt;folder&gt;"/><LineBreak/>
                                                <Run Text="GET_STATUS"/><LineBreak/>
                                                <Run Text="PING"/><LineBreak/>
                                                <Run Text="SEND &lt;event_data&gt;"/>
                                            </Paragraph>
                                        </RichTextBlock>
                                    </StackPanel>
                                </Expander>
                            </StackPanel>
                        </Border>
                    </Grid>
                </Grid>
            </StackPanel>

            <!-- 6. 外观 -->
            <StackPanel Spacing="8">
                <TextBlock x:Uid="SettingsPage_Personalization" Style="{ThemeResource SubtitleTextBlockStyle}"/>
                <Grid CornerRadius="8">
                    <Grid Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Translation="0,0,8">
                        <Grid.Shadow>
                            <ThemeShadow />
                        </Grid.Shadow>
                        <Border BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="16">
                            <StackPanel Spacing="20">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel VerticalAlignment="Center">
                                        <Grid ColumnSpacing="12">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <FontIcon Glyph="&#xE775;" FontSize="18"/>
                                            <StackPanel Grid.Column="1">
                                                <TextBlock x:Uid="SettingsPage_Language"/>
                                                <TextBlock x:Uid="SettingsPage_LanguageDesc" FontSize="12" Opacity="0.6"/>
                                            </StackPanel>
                                        </Grid>
                                    </StackPanel>
                                    <ComboBox x:Name="LanguageCombo" Grid.Column="1" Width="160" SelectionChanged="OnLanguageChanged">
                                        <ComboBoxItem x:Uid="SettingsPage_LanguageSystem"/>
                                        <ComboBoxItem x:Uid="SettingsPage_LanguageEnglish"/>
                                        <ComboBoxItem x:Uid="SettingsPage_LanguageChinese"/>
                                    </ComboBox>
                                </Grid>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel VerticalAlignment="Center">
                                        <Grid ColumnSpacing="12">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <FontIcon Glyph="&#xE790;" FontSize="18"/>
                                            <StackPanel Grid.Column="1">
                                                <TextBlock x:Uid="SettingsPage_AppTheme"/>
                                                <TextBlock x:Uid="SettingsPage_AppThemeDesc" FontSize="12" Opacity="0.6"/>
                                            </StackPanel>
                                        </Grid>
                                    </StackPanel>
                                    <!-- 添加 SelectionChanged 事件 -->
                                    <ComboBox Grid.Column="1" SelectedIndex="{x:Bind Settings.ThemeIndex, Mode=TwoWay}" Width="160" SelectionChanged="OnThemeChanged">
                                        <ComboBoxItem x:Uid="SettingsPage_ThemeDark"/>
                                        <ComboBoxItem x:Uid="SettingsPage_ThemeLight"/>
                                    </ComboBox>
                                </Grid>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel VerticalAlignment="Center">
                                        <Grid ColumnSpacing="12">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <FontIcon Glyph="&#xE8D2;" FontSize="18"/>
                                            <StackPanel Grid.Column="1">
                                                <TextBlock x:Uid="SettingsPage_FontFamily"/>
                                                <TextBlock x:Uid="SettingsPage_FontFamilyDesc" FontSize="12" Opacity="0.6"/>
                                            </StackPanel>
                                        </Grid>
                                    </StackPanel>
                                    <ComboBox x:Name="FontFamilyCombo" Grid.Column="1" Width="160" ItemsSource="{x:Bind FontFamilies}" SelectionChanged="OnFontFamilyChanged"/>
                                </Grid>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel VerticalAlignment="Center">
                                        <Grid ColumnSpacing="12">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <FontIcon Glyph="&#xE73E;" FontSize="18"/>
                                            <StackPanel Grid.Column="1">
                                                <TextBlock x:Uid="SettingsPage_FontSize"/>
                                                <TextBlock x:Uid="SettingsPage_FontSizeDesc" FontSize="12" Opacity="0.6"/>
                                            </StackPanel>
                                        </Grid>
                                    </StackPanel>
                                    <NumberBox x:Name="FontSizeBox" Grid.Column="1" Width="160" Minimum="12" Maximum="20" SmallChange="0.5" LargeChange="1" Value="{x:Bind Settings.BaseFontSize, Mode=TwoWay}" ValueChanged="OnFontSizeChanged"/>
                                </Grid>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel VerticalAlignment="Center">
                                        <Grid ColumnSpacing="12">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <FontIcon Glyph="&#xE700;" FontSize="18"/>
                                            <StackPanel Grid.Column="1">
                                                <TextBlock x:Uid="SettingsPage_NavPaneWidth"/>
                                                <TextBlock x:Uid="SettingsPage_NavPaneWidthDesc" FontSize="12" Opacity="0.6"/>
                                            </StackPanel>
                                        </Grid>
                                    </StackPanel>
                                    <NumberBox Grid.Column="1" Width="160" Minimum="160" Maximum="600" Value="{x:Bind Settings.NavPaneWidth, Mode=TwoWay}" SpinButtonPlacementMode="Inline" SmallChange="10" LargeChange="50" />
                                </Grid>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Spacing="6">
                                        <TextBlock x:Uid="SettingsPage_StartupSize" />
                                        <TextBlock x:Uid="SettingsPage_StartupSizeDesc" FontSize="12" Opacity="0.6" />
                                        <StackPanel Orientation="Horizontal" Spacing="12">
                                            <NumberBox x:Name="StartupWidthBox"
                                                       Width="100"
                                                       Minimum="640"
                                                       Maximum="3840"
                                                       SmallChange="20"
                                                       LargeChange="100"
                                                       Value="{x:Bind Settings.StartupWidth, Mode=TwoWay}"
                                                       ValueChanged="OnStartupSizeChanged"/>
                                            <TextBlock Text="×" VerticalAlignment="Center" Opacity="0.5" />
                                            <NumberBox x:Name="StartupHeightBox"
                                                       Width="100"
                                                       Minimum="480"
                                                       Maximum="2160"
                                                       SmallChange="20"
                                                       LargeChange="100"
                                                       Value="{x:Bind Settings.StartupHeight, Mode=TwoWay}"
                                                       ValueChanged="OnStartupSizeChanged"/>
                                            <TextBlock Text="px" VerticalAlignment="Center" Opacity="0.6" />
                                        </StackPanel>
                                    </StackPanel>
                                    <Button x:Uid="SettingsPage_ApplySize" Grid.Column="1" VerticalAlignment="Center" Click="OnApplyStartupSizeClick" />
                                </Grid>
                            </StackPanel>
                        </Border>
                    </Grid>
                </Grid>
            </StackPanel>

        </StackPanel>
    </ScrollViewer>
</Page>
